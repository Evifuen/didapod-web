name: Deploy
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: 'Login to Azure'
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 'Force Deploy V4'
        run: |
          az container create \
            --resource-group didapod \
            --name didapod-v4-app \
            --image midictaicontainer.azurecr.io/didapod:latest \
            --dns-name-label didapod-v4-2026 \
            --ports 80 \
            --registry-login-server midictaicontainer.azurecr.io \
            --registry-username midictaicontainer \
            --registry-password 9Vurt983vDBLkflxMkxINCvpNa56eGTtYFCXIYyLtD4PwxHcoDtnJQQJ99CBACYeBjFEqg7NAAACAZCRnjFQ \
            --location eastus \
            --os-type Linux \
            --cpu 1 \
            --memory 1.5
